# 3장

## 장 코드 발췌
이 장의 코드 스니펫은 `chapter_code_excerpts` 폴더에 등장 순서대로 정리되어 있다. 예: `00_error_class.py`
이 코드는 참고용으로 제공되며 실행을 목적으로 하지 않는다.

## 작업 관리 애플리케이션
이 장에는 작업 관리 애플리케이션 코드가 없다.

## 역자 추가 코드 예제 노트북

이 장의 코드 예제는 Jupyter 노트북(`Chapter_3_코드예제.ipynb`)으로도 제공된다. [루트 README](../README.md)의 Colab 배지를 클릭하면 바로 실행할 수 있다. 셀을 위에서 아래로 순서대로 실행한다.

- 노트북 코드는 책의 코드 발췌와 동일하되, 노트북 환경에서 실행 가능하도록 일부 수정되었다.
- 수정된 부분은 `[추가]`, `[수정]`, `[보완]` 주석으로 표시되어 있다.
- 일부 셀은 의도적으로 에러를 발생시키는 예제이다. 해당 셀 앞의 마크다운 설명을 참고한다.